/**
 * @deprecated Phase 3 — use campaignProgressMonitorCron.js (scheduler-only, no sync send loop).
 * Re-exported for legacy imports / coordinator bundles.
 */
module.exports = require('./campaignProgressMonitorCron');

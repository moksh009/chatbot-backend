"use strict";

/** Bump when Privacy Policy or Terms of Service materially change (must match frontend). */
const LEGAL_DOCS_VERSION = process.env.LEGAL_DOCS_VERSION || "2026-05-06";

module.exports = { LEGAL_DOCS_VERSION };

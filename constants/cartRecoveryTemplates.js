'use strict';

/** Canonical Meta template names for 3-step cart recovery ladder. */
const CART_RECOVERY_META_NAMES = ['cart_recovery_1', 'cart_recovery_2', 'cart_recovery_3'];

module.exports = { CART_RECOVERY_META_NAMES };

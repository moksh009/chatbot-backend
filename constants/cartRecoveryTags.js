'use strict';

/** CRM tag shown in Audience + Live Chat for abandoned checkout leads */
const ABANDONED_CART_TAG = 'Abandoned cart';

/** Applied when order matches after recovery messaging */
const RECOVERED_CART_TAG = 'Recovered cart';

module.exports = {
  ABANDONED_CART_TAG,
  RECOVERED_CART_TAG,
};
